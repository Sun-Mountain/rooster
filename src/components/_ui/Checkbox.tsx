import { Checkbox as CheckboxUI } from "@mui/material";

export const Checkbox = () => {
  return (
    <div className="checkbox-container">
      <CheckboxUI className="checkbox" />
    </div>
  )
};